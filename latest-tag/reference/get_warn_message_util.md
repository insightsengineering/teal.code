# Obtain warnings or messages from code slot

Obtain warnings or messages from code slot

## Usage

``` r
get_warn_message_util(object, what)
```

## Arguments

- object:

  (`qenv`)

- what:

  (`warning` or `message`)

## Value

`character(1)` containing combined message or `NULL` when no
warnings/messages
